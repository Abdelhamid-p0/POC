package com.service.payement.repository;

import com.service.payement.model.Odr;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OdrRepository extends JpaRepository<Odr, Long> {
}
